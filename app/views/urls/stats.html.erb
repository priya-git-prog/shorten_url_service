<!DOCTYPE html>
<html>
<body>
  <h2>Analytics</h2>
  <p class="links"><%= link_to "All URLs", "/" %></p>
  <p class="links"><%= link_to 'Create New', new_path %></p>
  <% host = request.host_with_port %>
  <% Url.all.each do |url|%>
    <hr>
    <h4>Long URL: <%= url.long_url %></h4>
    <h4>Shortened URL: <%= host + '/' + url.short_url %></h4>
    <h4>Total Clicks: <%= url.clicks%></h4>
    <h4>IP: <%= @ip_add%></h4>
    <h4>Location: <%= @location%></h4>
  <% end %>
</body>
</html>