<!DOCTYPE html>
<html>
  <body>
    <h1>Welcome to URL Shortening Service</h1>
    <h3>Enter Long URL</h3>
    <form action="/create", method="post">
      <input type="text" style="width: 40%" value="" name="long_url">
      <input type="submit" value="SHORTEN" >
    </form><br>

    <div>
      <h4>Long URL: <%= @long_url%></h4>
      <h4>Shortened URL: <%= link_to @short_url, "#{url_with_protocol(@short_url)}", target: "new" %></h4>
    </div><br>

    <p class="links"><%= link_to "All URLs", "/" %></p>
    <p class="links"><%= link_to "Get analysis", stats_path %></p>
  </body>
</html>
